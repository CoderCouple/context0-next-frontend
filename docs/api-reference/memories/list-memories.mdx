---
title: "List Memories"
api: "GET /api/v1/memory/"
description: "Retrieve all memories for the authenticated user"
---

## Overview

This endpoint returns a list of all memories belonging to the authenticated user. Memories are paginated and returned in reverse chronological order (newest first).

## Response

<ResponseField name="memories" type="array">
  Array of memory objects
  <Expandable title="Memory Object">
    <ResponseField name="id" type="string">
      Unique identifier for the memory
    </ResponseField>
    <ResponseField name="content" type="string">
      The memory content/text
    </ResponseField>
    <ResponseField name="metadata" type="object">
      Additional metadata associated with the memory
    </ResponseField>
    <ResponseField name="created_at" type="string">
      ISO timestamp when the memory was created
    </ResponseField>
    <ResponseField name="updated_at" type="string">
      ISO timestamp when the memory was last updated
    </ResponseField>
  </Expandable>
</ResponseField>

<RequestExample>

```bash cURL
curl -X GET "https://api.contextzero.ai/api/v1/memory/" \
  -H "Authorization: Bearer YOUR_API_TOKEN" \
  -H "Content-Type: application/json"
```

```javascript JavaScript
const response = await fetch('https://api.contextzero.ai/api/v1/memory/', {
  method: 'GET',
  headers: {
    'Authorization': 'Bearer YOUR_API_TOKEN',
    'Content-Type': 'application/json',
  },
});

const memories = await response.json();
```

```python Python
import requests

response = requests.get(
    "https://api.contextzero.ai/api/v1/memory/",
    headers={
        "Authorization": "Bearer YOUR_API_TOKEN",
        "Content-Type": "application/json"
    }
)

memories = response.json()
```

</RequestExample>

<ResponseExample>

```json Response
{
  "memories": [
    {
      "id": "mem_12345",
      "content": "Remember to follow up with the client about the project requirements.",
      "metadata": {
        "tags": ["work", "client"],
        "priority": "high"
      },
      "created_at": "2024-01-15T10:30:00Z",
      "updated_at": "2024-01-15T10:30:00Z"
    },
    {
      "id": "mem_12344",
      "content": "Meeting notes from the team standup.",
      "metadata": {
        "tags": ["meeting", "team"],
        "priority": "medium"
      },
      "created_at": "2024-01-14T09:15:00Z",
      "updated_at": "2024-01-14T09:15:00Z"
    }
  ]
}
```

</ResponseExample>